# String Validation

A console application that validates user input.

## Features

- Accepts a user's name
- Rejects empty input
- Rejects names longer than 12 characters
- Continues asking until the user types `exit`

## Concepts Practiced

- std::string
- std::getline()
- while loops
- break
- if / else if / else
- Input validation